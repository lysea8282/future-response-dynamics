# Semantic addressability boundary

The GRU full assay includes sign-flip, wrong-object and in-plane norm-matched controls. LSTM P2-aligned development found no common affine rank supported by at least 2/3 checkpoints: 390991 passes 6+,390992/390993 pass none in the tested grid. Oracle capacity passes all three at 6,8,12,16. This separates the tested mapper from privileged capacity; no result here rules out other semantic maps. Fresh LSTM transport did not test semantic specificity.
