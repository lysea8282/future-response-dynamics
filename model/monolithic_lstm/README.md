# Monolithic LSTM

494,664 parameters; complete carrier 384=[h;c], each component 192. `lstm_model.py` is a byte-identical architecture source. Rank 6 is the development-selected privileged correction rank. The full scientific src package is preserved under experiments/cross_architecture/lstm/src; configure it using the frozen transport contract before any separately authorized reproduction. Operational affine semantic addressability was not robustly supported.
